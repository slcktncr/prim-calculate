# MongoDB Bağlantı Bilgileri
MONGODB_URI=mongodb://localhost:27017/prim_calculate

# JWT Güvenlik Anahtarı (Güçlü bir anahtar kullanın)
JWT_SECRET=your_super_secure_jwt_secret_key_here_change_this_in_production

# Sunucu Portu
PORT=5000

# Ortam (development, production, test)
NODE_ENV=development

# MongoDB Kullanıcı Adı (Docker kullanıyorsanız)
# MONGO_USER=admin

# MongoDB Şifresi (Docker kullanıyorsanız)
# MONGO_PASSWORD=password123

# MongoDB Host (Docker kullanıyorsanız)
# MONGO_HOST=mongodb

# MongoDB Port (Docker kullanıyorsanız)
# MONGO_PORT=27017

# MongoDB Veritabanı Adı
# MONGO_DB=prim_calculate
